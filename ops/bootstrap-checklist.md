# Bootstrap Checklist

1. 绑定 GitHub 仓库
2. 验证 `gh auth status`
3. 配置远端 `main` 保护
4. 启用 required checks
5. 启用远端 Codex review 门禁
6. 运行 `scripts/check-governance.ps1`，或在 Linux/macOS 上运行 `scripts/check-governance.sh`
